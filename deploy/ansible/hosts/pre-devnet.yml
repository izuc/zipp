metrics:
  hosts:
    metrics-01.zipp.org:

supports:
  hosts:
    analysisentry-01.zipp.org:
      entryNodeSeed: "{{ ANALYSISSENTRY_01_ENTRYNODE_SEED }}"
      manaDashboardHost: "http://bootstrap-01.zipp.org:8081"
      logLevel: "debug"

zipps:
  children:
    internal_nodes:
      hosts:
        bootstrap-01.zipp.org:
          seed: "{{ BOOTSTRAP_01_SEED }}"
          bootstrap: true
        vanilla-01.zipp.org:
          seed: "{{ VANILLA_01_SEED }}"
        node-01.zipp.org:
          seed: "{{ NODE_01_SEED }}"
        node-02.zipp.org:
          seed: "{{ NODE_02_SEED }}"
        node-03.zipp.org:
          seed: "{{ NODE_03_SEED }}"
        node-04.zipp.org:
          seed: "{{ NODE_04_SEED }}"
        node-05.zipp.org:
          seed: "{{ NODE_05_SEED }}"
        faucet-01.zipp.org:
          seed: "{{ FAUCET_01_SEED }}"
          faucet: true
          faucetSeed: "{{ FAUCET_01_FAUCET_SEED }}"
  vars:
    entryNodeHost: "analysisentry-01.zipp.org"
    entryNodePubKey: "2PV5487xMw5rasGBXXWeqSi4hLz7r19YBt8Y1TGAsQbj"
    bootstrapNodePubKey: "Gm7W191NDnqyF7KJycZqK7V6ENLwqxTwoKQN4SmpkB24"
    remoteLoggerHost: "metrics-01.zipp.org"
    logLevel: "debug"
    debugPorts: true
    remoteLogs: true
